# Grafana Dashboard Provisioning
apiVersion: 1

providers:
  - name: 'Rent Ready Dashboards'
    orgId: 1
    folder: 'Rent Ready'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/yaml
